import NavBar from './Navigation'

// loop through the first 25 Items and dispaly them. 
// Create hooks/helpers for different filters and add that as an option for the right hand side. 


export default function Shop() { 
    return ( 
        <> 
        <NavBar />
        </>
    )
}